package club.multilab.music.server

import org.koin.dsl.module

val serverModule = module {
//    single<SearchForTracksAdapter> { SearchForTracksAdapter(get<MusicService>()::searchForTracks) }
//    single<HandleIntentAdapter> { HandleIntentAdapter(get<MusicService>()::handleIntent) }
}